# MCP Database Toolbox configuration for Chicago Crimes demo database
# Auto-generated by Plugable Chat
# Tool names match what Plugable Chat's DatabaseToolboxActor expects

sources:
  demo-sqlite:
    kind: sqlite
    database: "/Users/bernie/git/plugable-chat/test-data/demo.db"

tools:
  # Generic SQL execution tool - used by DatabaseToolboxActor for all queries
  sqlite-sql:
    kind: sqlite-sql
    source: demo-sqlite
    description: Execute a SQL query against the Chicago Crimes SQLite database
    statement: "{{.sql}}"
    templateParameters:
      - name: sql
        type: string
        description: The SQL query to execute
