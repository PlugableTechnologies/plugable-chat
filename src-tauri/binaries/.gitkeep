# This directory contains runtime binaries that must be bundled with the application
# The build.rs script automatically copies ONNX Runtime DLLs here during Windows builds
# These files are excluded from git via .gitignore but bundled in production builds
