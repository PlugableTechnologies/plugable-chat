[workspace]
members = ["src-tauri", "mcp-test-server"]
resolver = "2"

[profile.release]
codegen-units = 1
lto = true
strip = true
panic = "abort"

[profile.release.package."python-sandbox"]
opt-level = "s"
codegen-units = 1


